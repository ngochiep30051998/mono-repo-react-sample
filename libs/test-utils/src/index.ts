/**
 * Shared test utilities for the workspace.
 * Add render wrappers with providers, mock store factories, and mock service helpers here.
 * Example: export function renderWithProviders(ui: React.ReactElement, options?: {...}) { ... }
 */

export {};
